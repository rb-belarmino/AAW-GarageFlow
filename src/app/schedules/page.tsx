import { ScheduleConfigurator } from "@/components/schedules/ScheduleConfigurator";

export const dynamic = "force-dynamic";

export default function SchedulesPage() {
  return <ScheduleConfigurator />;
}
